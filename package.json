{
  "name": "oss-sahayak",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "shared",
    "backend",
    "ingestion-worker",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=backend",
    "dev:worker": "npm run dev --workspace=ingestion-worker",
    "dev:frontend": "npm run dev --workspace=frontend"
  }
}
